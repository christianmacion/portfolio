---
/**
 * index.astro — v7.5 HOME — TWO-PILLAR RESTRUCTURE
 *
 * 7-section structure (top → bottom):
 *   1. NewsRibbon — activity ticker (Lane 1) + methodology frames (Lane 2)
 *   2. Hero       — identity + role + Odometer KPIs + dual-path CTA + portrait
 *   3. Two-pillar row — EquityCurveChart (quant) ⇆ AgentGraph compact (AI)
 *   4. Marquee    — methodology frame loop
 *   5. ArchitectureBrief — STELLA-public-safe multi-agent chrome (slot — Phase D)
 *   6. Fit        — education + credentials
 *   7. Contact    — dual-path CTA mirrors hero
 *
 * v7.5 — adds a 7th section (ArchitectureBrief) carrying the
 * STELLA-public-safe architecture slice. The brief itself is implemented
 * in Phase D; this file declares the structural slot so the new section
 * sits in the right place from day one.
 */

import BaseLayout from '@layouts/BaseLayout.astro';
import { profile, archSafe } from '@utils/profile';
import { path } from '@utils/url';
import NewsRibbon from '@components/NewsRibbon.astro';
import Marquee from '@components/Marquee.astro';
import Odometer from '@components/Odometer.astro';
import EquityCurveChart from '@components/EquityCurveChart.astro';
import AgentGraph from '@components/AgentGraph.astro';
import ArchitectureBrief from '@components/ArchitectureBrief.astro';
import SwarmScene from '@components/SwarmScene.astro';
import LiveStats from '@components/LiveStats.astro';
import LiveActivity from '@components/LiveActivity.astro';

// === HERO KPIs (v7.5 — sourced from profile.stats; correct numbers)
// 9 quant strategies + 6 AI projects + 76.5k LOC Python + 31 eval gates
const QUANT_STRATEGIES = Number(profile.stats.strategyCount); // 9
const AI_PROJECTS = 6; // /content/projects/ai/*.mdx count
const LOC_PYTHON_K = 76.5; // thousands of LOC Python
const EVAL_GATES = Number(profile.stats.evalGates); // 31

// === TWO-PILLAR DATA — equity series (BTC/USDT paper-trade, 49 trading days)
// Deterministic — same series used on /positions.astro.
const equitySeries = [
  100.0, 100.4, 99.8, 100.9, 101.2, 100.7, 101.5, 102.1, 101.8, 102.4, 102.0, 101.4, 100.9, 101.6,
  102.3, 102.7, 103.1, 102.5, 101.9, 102.2, 102.8, 103.4, 102.9, 103.6, 104.2, 103.8, 104.5, 105.1,
  104.6, 105.3, 105.9, 106.4, 105.8, 106.6, 107.2, 107.8, 108.3, 107.6, 108.5, 109.2, 109.8, 109.1,
  109.9, 110.5, 111.0, 110.4, 111.2, 111.9, 111.3, 112.0,
];

const email = profile.contact.email;
const mailto = `mailto:${email}?subject=QR%20%2F%20AI%20role%20%E2%80%94%20hiring%20inquiry`;
---

<BaseLayout
  title={profile.titles.primary}
  description={profile.titles.secondary}
  surface="v6-13"
  theme="light"
  breadcrumbs={[{ name: 'Home', href: '/' }]}
>
  <!-- ============================================================
       1. NEWS-RIBBON — first chrome, "this site is updated" signal
       v7.4 motion layer (Lane 1 = updates, Lane 2 = methodology frames).
       ============================================================ -->
  <NewsRibbon />

  <!-- ============================================================
       2. HERO — identity + role + KPIs + dual-path CTA + portrait
       v7.5 — KPIs now use Odometer with proper format() prop
       (was data-counter="7300" → "7300" stripped of comma + suffix).
       Dual-path CTA row replaces single CTA: EMAIL + OPEN DESK + READ LOOP.
       ============================================================ -->
  <section class="hero" aria-label="Identity">
    <div class="container hero__inner">
      <p class="hero__eyebrow mono" data-stagger="1">
        Q3 2026 · OPEN TO REMOTE QR / AI ROLES · &lt;24H REPLY
      </p>

      <div class="hero__grid">
        <div class="hero__identity">
          <h1 class="hero__name" data-stagger="2">
            <span class="word">Christian</span>
            <span class="word">T.</span>
            <span class="word">Macion</span>
          </h1>
          <p class="hero__role" data-stagger="3">
            Quantitative Researcher · AI Engineer · UTC+8 · Digos, PH<span
              class="hero__cursor"
              aria-hidden="true"></span>
          </p>

          <p class="hero__lede" data-stagger="4">
            <em>I build research systems where the evals prove the work.</em>
            {profile.stats.aiAgentCount}-agent <b>STELLA</b> swarm watches the world — every central-bank
            decision, every earnings call, every GDELT signal — and turns it into a research signal the
            trading desk can defend. Senior practitioner across quant, AI engineering, and multi-agent
            build. I ship the eval harness, the backtest manifest, and the audit gate — so the work holds
            up in interview.
          </p>

          <ul class="hero__kpis" aria-label="Key metrics" data-stagger="5">
            <li class="hero__kpi">
              <Odometer
                value={QUANT_STRATEGIES}
                label="Quant strategies"
                class="hero__kpi-odometer"
              />
            </li>
            <li class="hero__kpi">
              <Odometer value={AI_PROJECTS} label="AI projects" class="hero__kpi-odometer" />
            </li>
            <li class="hero__kpi">
              <Odometer
                value={LOC_PYTHON_K}
                format={(n) => `${n.toFixed(1)}k`}
                label="LOC Python"
                class="hero__kpi-odometer"
              />
            </li>
            <li class="hero__kpi">
              <Odometer value={EVAL_GATES} label="Eval gates" class="hero__kpi-odometer" />
            </li>
          </ul>

          <div class="hero__cta-row" data-stagger="6">
            <a class="cta cta--primary" href={mailto}>EMAIL — HIRING INQUIRY</a>
            <a class="cta cta--ghost" href={path('/desk')}>OPEN DESK →</a>
            <a class="cta cta--ghost" href={path('/ai')}>READ LOOP →</a>
            <a class="cta cta--ghost" href={path('/for-recruiters')}>HR SKIM →</a>
          </div>
        </div>

        <aside class="hero__portrait" data-stagger="3">
          <picture>
            <source type="image/webp" srcset="/headshot.webp" />
            <img
              src={profile.headshot.src}
              alt={profile.headshot.alt}
              width="280"
              height="320"
              loading="eager"
            />
          </picture>
          <p class="hero__portrait-credit mono">{profile.headshot.credit}</p>
        </aside>
      </div>
    </div>
  </section>

  <!-- ============================================================
       2b. STELLA LIVE LOOP — world-class animated demonstration.
       Owner feedback 2026-07-26: "make like an animated video of
       stella- AI assistant working with us with world class data
       and add it to the site. vide one loop that keeps playing to
       show amazing visual. world class presentation visual. make
       stella look amazing." Pure SVG + CSS keyframes; loops forever.
       ============================================================ -->
  <SwarmScene />
  <LiveStats />
  <LiveActivity />

  <!-- ============================================================
       3. TWO-PILLAR ROW — 1fr 1fr above 900px, stacks mobile.
       Left pane = quant lane (EquityCurveChart).
       Right pane = AI lane (AgentGraph compact = 7 nodes, drops inputs).
       Each pane carries: eyebrow · viz · 3 micro-stats · read-pillar link.
       ============================================================ -->
  <section
    class="pillars"
    id="pillars"
    aria-label="Two-pillar positioning: Quantitative Research × AI Systems"
  >
    <div class="container">
      <p class="pillars__eyebrow mono" data-reveal="lift-sm">
        TWO PILLARS · ONE OPERATOR · SAME EVAL DISCIPLINE
      </p>

      <div class="pillars__grid">
        <article class="pillar pillar--quant" data-reveal>
          <p class="pillar__lane mono">
            QUANT LANE · {profile.stats.strategyCount} STRATEGIES · {profile.stats.locPython} LOC PYTHON
          </p>
          <div class="pillar__viz">
            <EquityCurveChart
              data={equitySeries}
              width={720}
              height={260}
              volWindow={5}
              title="BTC/USDT · live paper-trade · vol-target momentum"
              caption="Live series · 49 trading days · 5 bps/side slippage · vol target 30% annualized"
            />
          </div>
          <ul class="pillar__stats mono">
            <li>
              <b class="tnum"
                ><span class="pillar__count" data-count="0.39" data-decimals="2">0.00</span></b
              ><span>SHARPE · ANN.</span>
            </li>
            <li>
              <b class="tnum"
                ><span
                  class="pillar__count"
                  data-count="-3.6"
                  data-decimals="1"
                  data-suffix="%"
                  data-prefix="−">−0.0%</span
                ></b
              ><span>MAX DRAWDOWN</span>
            </li>
            <li>
              <b class="tnum"
                ><span
                  class="pillar__count"
                  data-count="12"
                  data-decimals="0"
                  data-suffix="%"
                  data-prefix="+">+0%</span
                ></b
              ><span>NET RETURN</span>
            </li>
          </ul>
          <p class="pillar__cta">
            <a href={`${path('/projects')}?tag=quant`}>READ QUANT PILLAR →</a>
          </p>
        </article>

        <article class="pillar pillar--ai" data-reveal data-reveal-delay="120">
          <p class="pillar__lane mono">
            AI LANE · {AI_PROJECTS} PROJECTS · {archSafe.agentCount}-AGENT OFFICE
          </p>
          <div class="pillar__viz">
            <AgentGraph compact />
          </div>
          <ul class="pillar__stats mono">
            <li>
              <b class="tnum"
                ><span
                  class="pillar__count"
                  data-count={profile.stats.aiAgentCount}
                  data-decimals="0">0</span
                ></b
              ><span>DAG AGENTS</span>
            </li>
            <li>
              <b class="tnum"
                ><span class="pillar__count" data-count={archSafe.subTeamCount} data-decimals="0"
                  >0</span
                ></b
              ><span>SUB-TEAMS</span>
            </li>
            <li>
              <b class="tnum"
                ><span class="pillar__count" data-count={archSafe.mustHaveCount} data-decimals="0"
                  >0</span
                ></b
              ><span>MUST-HAVES</span>
            </li>
          </ul>
          <p class="pillar__cta">
            <a href={path('/ai')}>READ AI PILLAR →</a>
          </p>
        </article>
      </div>
    </div>
  </section>

  <!-- ============================================================
       4. METHODOLOGY MARQUEE — frame loop between pillars and arch brief
       v7.5 — corrected DEFALTED → DEFLATED · name-drop frame replaced
       with OPS-RESEARCH.
       ============================================================ -->
  <Marquee
    items={[
      {
        head: 'EVAL-FIRST',
        tail: `${profile.stats.evalGates} GATES · ZERO ROLLBACKS`,
        accent: 'warn',
      },
      { head: 'NDA-CLEAN', tail: 'PUBLIC-DATA REPRODUCIBLE', accent: 'sage' },
      {
        head: 'ALPHA-DRIVEN',
        tail: `${profile.stats.strategyCount} STRATEGIES · SAME METHOD`,
        accent: 'sage',
      },
      { head: 'DEFLATED SHARPE', tail: 'OUT-OF-SAMPLE PENALTY', accent: 'terracotta' },
      { head: 'BLOCK-BOOTSTRAP', tail: 'PBO-TESTED · OOS-VALIDATED', accent: 'sage' },
      {
        head: 'MULTI-AGENT',
        tail: `${profile.stats.aiAgentCount} AGENTS · 1 ORCHESTRATOR`,
        accent: 'violet',
      },
      { head: 'COINTEGRATION', tail: 'PAIRS · 5Y HALF-LIFE', accent: 'sage' },
      { head: 'VARIANCE RISK', tail: 'PREMIUM HARVEST · DELTA-HEDGED', accent: 'warn' },
      { head: 'REGIME OVERLAY', tail: 'VOL-TARGETED · DEFENSIVE', accent: 'terracotta' },
      { head: 'CRYPTO NATIVE', tail: 'BTC · ETH · FUNDING-RATE CARRY', accent: 'warn' },
      { head: 'LOOKAHEAD AUDIT', tail: '88% CATCH ON PHANTOM LEAK', accent: 'sage' },
      { head: 'OPS-RESEARCH', tail: 'MULTI-AGENT · EVAL-DRIVEN · NDA-CLEAN', accent: 'sage' },
    ]}
    speedSec={64}
    trailing="methodology frames · 12 anchor disciplines"
  />

  <!-- ============================================================
       5. ARCHITECTURE BRIEF — STELLA-public-safe slice.
       v7.5 — Phase D wired in the <ArchitectureBrief /> component.
       Numbers come from `profile.archSafe` (NDA-clean: counts + taxonomy
       only). No brand, officer names, or chrome leaks.
       ============================================================ -->
  <ArchitectureBrief />

  <!-- ============================================================
       5b. AI HISTORY — 2022 → 2026 prompt-to-graph timeline.
       v7.6 — surfaces the 5-rung spine as a public-facing story.
       Each rung is a year, a layer, a one-line summary, and a
       BOTTOM LINE. Closes with the STELLA callout + workbooks
       CTA. NDA-clean — describes the public arc, not the org.
       ============================================================ -->
  <section class="ai-history" id="ai-history" aria-label="AI history 2022 to 2026">
    <div class="container">
      <p class="ai-history__eyebrow mono" data-reveal="lift-sm">
        AI ENGINEERING · 2022 → 2026 · THE FIVE-RUNG SPINE
      </p>
      <h2 class="ai-history__title" data-words data-reveal>
        Prompt <span class="hl">→</span> skills <span class="hl">→</span> agents <span class="hl"
          >→</span
        > loops <span class="hl">→</span> graphs.
      </h2>
      <p class="ai-history__lede" data-reveal>
        Five compounding layers — adapted every AI era, including agent swarms. Most teams plateau
        at the loop rung. The graph rung is the productivity unlock — and it's where I live.
      </p>

      <ol class="ai-history__rail" data-reveal>
        <li class="ai-rung ai-rung--prompt">
          <span class="ai-rung__year mono">2022</span>
          <h3 class="ai-rung__name">Prompt engineering</h3>
          <p class="ai-rung__sub">
            GPT-3 + instruction tuning. One prompt, one output. ~5 layers (system · context ·
            instruction · output schema · evidence).
          </p>
          <p class="ai-rung__line mono">/ everyone</p>
        </li>
        <li class="ai-rung ai-rung--chatgpt">
          <span class="ai-rung__year mono">2023</span>
          <h3 class="ai-rung__name">ChatGPT + RAG</h3>
          <p class="ai-rung__sub">
            ChatGPT public (Nov 2022). Retrieval-Augmented Generation goes mainstream. Vector
            databases, LangChain. Knowledge cutoff is the wall.
          </p>
          <p class="ai-rung__line mono">/ builder</p>
        </li>
        <li class="ai-rung ai-rung--agent">
          <span class="ai-rung__year mono">2024</span>
          <h3 class="ai-rung__name">Agents + tools</h3>
          <p class="ai-rung__sub">
            Function calling. Auto-GPT. LangGraph. The agent loop becomes a thing. Context windows
            are still the wall — agents die mid-task.
          </p>
          <p class="ai-rung__line mono">/ researcher</p>
        </li>
        <li class="ai-rung ai-rung--skill">
          <span class="ai-rung__year mono">2025</span>
          <h3 class="ai-rung__name">Skills + loops</h3>
          <p class="ai-rung__sub">
            Anthropic's Agent Skills (progressive disclosure). Karpathy's autoresearch loop (700
            experiments). The 5-must-have contract. Eval-gate enforcement.
          </p>
          <p class="ai-rung__line mono">/ engineer</p>
        </li>
        <li class="ai-rung ai-rung--graph">
          <span class="ai-rung__year mono">2026</span>
          <h3 class="ai-rung__name">Graphs · STELLA</h3>
          <p class="ai-rung__sub">
            Typed entities + relations + provenance. Shared memory across agents. Routing files.
            Karpathy's LLM Wiki. The graph rung — where 53-agent offices live.
          </p>
          <p class="ai-rung__line mono">/ architect · STELLA</p>
        </li>
      </ol>

      <p class="ai-history__closer" data-reveal>
        <b>You are here.</b> The spine is what I build on — and what I publish in the
        {' '}<a href={path('/workbooks')}>workbook trilogy</a>{' '}.
      </p>
    </div>
  </section>

  <!-- ============================================================
       6. FIT — education + credentials (compact, no change)
       ============================================================ -->
  <section class="fit" id="fit" aria-label="Education and credentials">
    <div class="container">
      <p class="fit__eyebrow mono" data-reveal="lift-sm">EDUCATION · CREDENTIALS · FIT</p>
      <p class="fit__line" data-reveal>
        <b>PSHS-SMC</b> · <b>USeP</b> · <b>University of Mindanao</b>
        <span class="fit__sep">·</span>
        <b>STA</b> · <b>BIDA</b> · NASA · BMC · HF · UP · OJP · Crypto­neers
      </p>
      <p class="fit__line fit__line--meta" data-reveal="fade">
        NDA-clean by construction · eval-first · quant + AI · Digos City, Davao del Sur, PH
      </p>
    </div>
  </section>

  <!-- ============================================================
       7. CONTACT — dual-path CTA mirrors hero (same 3 buttons)
       ============================================================ -->
  <section class="contact" id="contact" aria-label="Contact">
    <div class="container contact__inner">
      <p class="contact__eyebrow mono" data-reveal="lift-sm">
        <span class="status-dot" aria-hidden="true"></span>READY WHEN YOU ARE.
      </p>
      <div class="contact__cta-row" data-reveal>
        <a class="cta cta--primary" href={mailto}>EMAIL — HIRING INQUIRY</a>
        <a class="cta cta--ghost" href={path('/desk')}>OPEN DESK →</a>
        <a class="cta cta--ghost" href={path('/ai')}>READ LOOP →</a>
      </div>
      <p class="contact__secondary mono" data-reveal="fade">
        <a href="https://github.com/christianmacion26">GitHub</a>
        <span class="fit__sep">·</span>
        <a href="https://linkedin.com/in/christianmacion">LinkedIn</a>
        <span class="fit__sep">·</span>
        <a href={`mailto:${email}`}>{email}</a>
      </p>

      {/* v7.22 — signature line + "what I'm working on next" closes the page */}
      <div class="contact__closer" data-reveal="lift-sm">
        <p class="contact__signature">
          <span class="contact__signature-name">— Christian T. Macion</span>
          <span class="contact__signature-meta">
            Quant Researcher · AI Engineer · Digos City, PH
          </span>
        </p>
        <p class="contact__next mono">
          <span class="contact__next-tag">NEXT</span>
          Shipping graph engineering as the integrating layer + a recruiter-grade summary stack. Reply
          lands within 24h.
        </p>
      </div>
    </div>
  </section>
</BaseLayout>

<script>
  /* v7.6.8 — pillar count-up. Reads target value + format from data-*
     attributes on `.pillar__count` so the markup stays readable.
     Fires once on first scroll-in via IntersectionObserver. No
     Math.random / Date.now — deterministic easing curve. */
  (() => {
    const counters = document.querySelectorAll<HTMLElement>('.pillar__count');
    if (!counters.length) return;

    const prefersReduce = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    if (prefersReduce) {
      // Just write the final value; skip animation.
      counters.forEach((el) => {
        const target = parseFloat(el.dataset.count ?? '0');
        const decimals = parseInt(el.dataset.decimals ?? '0', 10);
        const suffix = el.dataset.suffix ?? '';
        const prefix = el.dataset.prefix ?? '';
        el.textContent = `${prefix}${target.toFixed(decimals)}${suffix}`;
      });
      return;
    }

    const easeOutCubic = (t: number) => 1 - Math.pow(1 - t, 3);

    function format(value: number, decimals: number, prefix: string, suffix: string) {
      return `${prefix}${value.toFixed(decimals)}${suffix}`;
    }

    function animate(el: HTMLElement) {
      const target = parseFloat(el.dataset.count ?? '0');
      const decimals = parseInt(el.dataset.decimals ?? '0', 10);
      const suffix = el.dataset.suffix ?? '';
      const prefix = el.dataset.prefix ?? '';

      const duration = 1400;
      const start = performance.now();
      const initial = 0;
      function step(now: number) {
        const elapsed = now - start;
        const t = Math.min(1, elapsed / duration);
        const v = initial + (target - initial) * easeOutCubic(t);
        el.textContent = format(v, decimals, prefix, suffix);
        if (t < 1) requestAnimationFrame(step);
        else el.textContent = format(target, decimals, prefix, suffix);
      }
      requestAnimationFrame(step);
    }

    const grid = document.querySelector('.pillars__grid');
    const seen = new WeakSet<HTMLElement>();
    const observer = new IntersectionObserver(
      (entries) => {
        for (const e of entries) {
          if (!e.isIntersecting) continue;
          const el = e.target as HTMLElement;
          if (seen.has(el)) continue;
          seen.add(el);
          animate(el);
        }
      },
      { threshold: 0.4, rootMargin: '0px 0px -10% 0px' },
    );
    counters.forEach((el) => observer.observe(el));
    // mark grid once any counter has fired (for the amber glow)
    const gridObserver = new IntersectionObserver(
      (entries) => {
        for (const e of entries) {
          if (e.isIntersecting) {
            (e.target as HTMLElement).classList.add('is-counted');
            gridObserver.disconnect();
            break;
          }
        }
      },
      { threshold: 0.4 },
    );
    if (grid) gridObserver.observe(grid);
  })();
</script>

<style>
  /* ============================================================
     v7.5 INSTITUTIONAL HOME — two-pillar restructure
     ============================================================ */

  /* Container — single column, generous gutter */
  .container {
    max-width: 1080px;
    margin: 0 auto;
    padding: 0 24px;
  }

  .hero__kpis {
    position: relative;
  }

  /* ====== HERO ====== */
  .hero {
    border-bottom: 1px solid var(--c-rule);
  }
  .hero__inner {
    padding: 48px 0 56px;
  }
  .hero__eyebrow {
    font: 500 11px / 1.4 var(--ff-mono);
    color: var(--c-warn);
    letter-spacing: 0.14em;
    text-transform: uppercase;
    margin: 0 0 28px;
  }
  .hero__grid {
    display: grid;
    grid-template-columns: 1fr 280px;
    gap: 56px;
    align-items: start;
  }
  .hero__identity {
    min-width: 0;
  }

  .hero__name {
    font: 600 48px / 1 var(--serif-display);
    color: var(--c-ink);
    letter-spacing: -0.024em;
    margin: 0 0 8px;
    display: flex;
    flex-wrap: wrap;
    align-items: baseline;
    gap: 12px;
  }
  .hero__name .word {
    display: inline-block;
  }
  .hero__name .word:nth-child(2),
  .hero__name .word:nth-child(3) {
    /* v7.6.1 — Owner feedback 2026-07-26: "make it same color the entire
       Christian T. Macion — black because it is my name." Dropped the
       gold --c-phi override; T. Macion now inherits the default ink
       color from .hero__name (var(--c-ink) = #1c2538 light / #d4cebd
       dark). Italic + 500 weight retained for the serif distinction. */
    font-style: italic;
    font-weight: 500;
  }

  .hero__role {
    font: 500 14px / 1.5 var(--serif-display);
    color: var(--c-ink-2);
    font-style: italic;
    margin: 0 0 24px;
  }

  .hero__lede {
    font: 400 16px / 1.6 var(--ff-sans);
    color: var(--c-ink);
    margin: 0 0 28px;
    max-width: 60ch;
  }
  .hero__lede em {
    font-family: var(--serif-display);
    font-style: italic;
    color: var(--c-warn);
    font-weight: 600;
    font-size: 17px;
  }

  /* === Hero KPIs — 4 across (was 3) ===
     Uses <Odometer> component so values animate + format
     correctly (76.5k for LOC, etc). Was raw data-counter strings
     that the odometer driver overwrote without re-formatting. */
  .hero__kpis {
    list-style: none;
    padding: 14px 0;
    margin: 0 0 32px;
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 16px;
    border-top: 1px solid var(--c-rule);
    border-bottom: 1px solid var(--c-rule);
  }
  .hero__kpi {
    display: flex;
    flex-direction: column;
    gap: 4px;
    min-width: 0;
  }
  /* Inherit Odometer's tabular-num + accent color from the component;
     only override the label to match the home's chrome (uppercase, mono). */
  .hero__kpi :global(.odometer__value) {
    color: var(--c-phi);
    font: 600 28px / 1 var(--serif-display);
    letter-spacing: -0.022em;
  }
  .hero__kpi :global(.odometer__label) {
    font: 500 10px / 1.2 var(--ff-mono);
    color: var(--c-ink-2);
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  /* === Dual-path CTA row === */
  .hero__cta-row {
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
    margin: 0;
    align-items: center;
  }

  .hero__portrait {
    margin: 0;
    text-align: center;
  }
  .hero__portrait img {
    width: 100%;
    height: auto;
    border-radius: 2px;
    /* v7.5 — dropped the 8px 24px rgba halo shadow (audit C04 tell).
       Single 1px hairline rule replaces it; portrait now sits flat on
       the page surface like a print headshot, not a UI card. */
    border: 1px solid var(--c-rule);
    display: block;
  }
  .hero__portrait-credit {
    font-size: 9.5px;
    letter-spacing: 0.1em;
    color: var(--c-ink-3);
    margin: 12px 0 0;
    text-transform: uppercase;
  }

  /* ====== CTA (shared) ====== */
  .cta {
    display: inline-block;
    padding: 14px 24px;
    font: 600 11px / 1 var(--ff-mono);
    letter-spacing: 0.16em;
    text-transform: uppercase;
    text-decoration: none;
    border: 1px solid var(--c-rule);
    cursor: pointer;
    transition:
      background 200ms ease,
      color 200ms ease,
      border-color 200ms ease;
    background: transparent;
    color: var(--c-ink);
  }
  .cta--primary {
    background: var(--c-warn);
    color: var(--c-bg);
    border-color: var(--c-warn);
  }
  .cta--primary:hover {
    background: var(--c-amber-deep);
    border-color: var(--c-amber-deep);
  }
  .cta--ghost {
    background: transparent;
    color: var(--c-ink);
    border-color: var(--c-rule);
  }
  .cta--ghost:hover {
    color: var(--c-warn);
    border-color: var(--c-warn);
  }

  /* ====== PILLARS — two-column grid ====== */
  .pillars {
    padding: 56px 0 64px;
    border-bottom: 1px solid var(--c-rule);
    background: var(--c-bg-2);
  }
  /* v7.6.8 — count-up. Tabular-num already on .tnum; we add a
     subtle flip on the <b> wrapper so the digit locks into place. */
  .pillar__count {
    display: inline-block;
    min-width: 1ch;
    transition: filter 0.4s ease;
  }
  .pillars__grid.is-counted .pillar__count {
    filter: drop-shadow(0 0 6px color-mix(in oklab, var(--c-amber) 40%, transparent));
  }
  @media (prefers-reduced-motion: reduce) {
    .pillar__count {
      transition: none;
      filter: none !important;
    }
  }
  .pillars__eyebrow {
    font: 500 11px / 1.4 var(--ff-mono);
    color: var(--c-warn);
    letter-spacing: 0.14em;
    text-transform: uppercase;
    margin: 0 0 28px;
  }
  .pillars__grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 32px;
  }
  .pillar {
    background: var(--c-bg);
    border: 1px solid var(--c-rule);
    padding: 24px;
  }
  .pillar__lane {
    font: 500 10px / 1.4 var(--ff-mono);
    color: var(--c-ink-3);
    letter-spacing: 0.14em;
    text-transform: uppercase;
    margin: 0 0 16px;
    padding-bottom: 12px;
    border-bottom: 1px solid var(--c-rule);
  }
  .pillar--ai .pillar__lane {
    color: var(--c-amber);
  }
  .pillar__viz {
    margin: 0 0 20px;
  }
  .pillar__viz :global(.eq-chart),
  .pillar__viz :global(.agent-graph) {
    margin: 0;
  }
  .pillar__stats {
    list-style: none;
    padding: 16px 0;
    margin: 0 0 16px;
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 12px;
    border-top: 1px solid var(--c-rule);
    border-bottom: 1px solid var(--c-rule);
  }
  .pillar__stats li {
    display: flex;
    flex-direction: column;
    gap: 4px;
  }
  .pillar__stats b {
    font: 600 20px / 1 var(--serif-display);
    color: var(--c-ink);
    letter-spacing: -0.018em;
  }
  .pillar__stats span {
    font: 500 9.5px / 1.2 var(--ff-mono);
    color: var(--c-ink-2);
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }
  .pillar__cta {
    margin: 0;
  }
  .pillar__cta a {
    font: 500 11px / 1.4 var(--ff-mono);
    color: var(--c-ink);
    letter-spacing: 0.14em;
    text-transform: uppercase;
    text-decoration: none;
    padding-bottom: 2px;
    border-bottom: 1px solid var(--c-rule);
    transition:
      color 200ms ease,
      border-color 200ms ease;
  }
  .pillar__cta a:hover {
    color: var(--c-warn);
    border-bottom-color: var(--c-warn);
  }

  /* ====== ARCHITECTURE BRIEF (rendered by ArchitectureBrief.astro) ====== */
  /* Local overrides intentionally minimal — ArchitectureBrief.astro owns
     its own scoped styles. Reserve space in the page flow only. */
  .arch-brief {
    margin: 0;
  }

  /* ====== Fit (education) ====== */
  .fit {
    padding: 40px 0;
    border-bottom: 1px solid var(--c-rule);
  }
  .fit__eyebrow {
    font: 500 11px / 1.4 var(--ff-mono);
    color: var(--c-ink-3);
    letter-spacing: 0.14em;
    text-transform: uppercase;
    margin: 0 0 16px;
  }
  .fit__line {
    font: 500 13px / 1.6 var(--ff-mono);
    color: var(--c-ink);
    margin: 0 0 8px;
  }
  .fit__line b {
    font-weight: 700;
    color: var(--c-ink);
  }
  .fit__line--meta {
    font: 400 12px / 1.6 var(--ff-sans);
    color: var(--c-ink-2);
    font-style: italic;
    margin: 0;
  }
  .fit__sep {
    color: var(--c-ink-3);
    margin: 0 8px;
  }

  /* ====== Contact — no gradient (audit C06 tell) ====== */
  .contact {
    padding: 56px 0;
    background: var(--c-bg);
    border-bottom: 1px solid var(--c-rule);
  }
  .contact__inner {
    text-align: center;
  }
  .contact__eyebrow {
    font: 600 20px / 1.2 var(--serif-display);
    font-style: italic;
    color: var(--c-ink);
    margin: 0 0 8px;
  }
  .contact__cta-row {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 12px;
    margin: 24px 0 20px;
  }
  .contact__secondary {
    font: 500 12px / 1.6 var(--ff-mono);
    color: var(--c-ink-2);
    margin: 0;
  }
  .contact__secondary a {
    color: var(--c-ink);
    text-decoration: none;
  }
  .contact__secondary a:hover {
    color: var(--c-warn);
  }

  /* === v7.22 — signature + "what's next" closer ============================= */
  .contact__closer {
    margin: 32px 0 0;
    padding: 16px 0 0;
    border-top: 1px dashed var(--c-rule);
  }
  .contact__signature {
    margin: 0 0 8px;
    font-family: var(--serif-display);
    font-size: 1.05rem;
    font-style: italic;
    color: var(--c-ink);
    line-height: 1.4;
  }
  .contact__signature-name {
    color: var(--c-amber);
    font-weight: 500;
    margin-right: 12px;
  }
  .contact__signature-meta {
    color: var(--c-ink-3);
    font-size: 0.85rem;
    font-style: normal;
    letter-spacing: 0.02em;
  }
  .contact__next {
    margin: 0;
    font: 500 11px / 1.6 var(--ff-mono);
    color: var(--c-ink-2);
    letter-spacing: 0.06em;
    text-transform: none;
  }
  .contact__next-tag {
    display: inline-block;
    margin-right: 8px;
    padding: 2px 6px;
    font: 600 9px / 1 var(--ff-mono);
    letter-spacing: 0.14em;
    color: var(--c-amber);
    border: 1px solid var(--c-amber);
    border-radius: 2px;
    text-transform: uppercase;
  }

  /* ====== Responsive ====== */
  @media (max-width: 900px) {
    .pillars__grid {
      grid-template-columns: 1fr;
      gap: 24px;
    }
  }

  @media (max-width: 720px) {
    .hero__inner {
      padding: 32px 0 40px;
    }
    .hero__grid {
      grid-template-columns: 1fr;
      gap: 24px;
    }
    .hero__portrait {
      order: -1;
      max-width: 200px;
      margin: 0 auto;
    }
    .hero__name {
      font-size: 36px;
    }
    .hero__name .word:nth-child(2),
    .hero__name .word:nth-child(3) {
      font-size: 32px;
    }
    .hero__kpis {
      grid-template-columns: repeat(2, 1fr);
      gap: 14px 12px;
    }
    .hero__kpi :global(.odometer__value) {
      font-size: 22px;
    }
    .pillar__stats {
      grid-template-columns: repeat(3, 1fr);
      gap: 8px;
    }
    .pillar__stats b {
      font-size: 16px;
    }
    .pillar__stats span {
      font-size: 9px;
      letter-spacing: 0.1em;
    }
    .contact__eyebrow {
      font-size: 18px;
    }
  }

  /* ============================================================
     v7.6 — AI HISTORY TIMELINE (2022 → 2026)
     5-rung spine as a public-facing story. Surfaces STELLA +
     graph engineering alongside the prompt→graph journey.
     ============================================================ */
  .ai-history {
    padding-block: var(--sp-9);
    background: var(--c-bg);
    border-top: var(--border-paper);
    border-bottom: var(--border-paper);
  }
  .ai-history > .container {
    max-width: 1180px;
  }
  .ai-history__eyebrow {
    font: 500 11px / 1.4 var(--ff-mono);
    color: var(--c-amber);
    letter-spacing: 0.18em;
    text-transform: uppercase;
    margin: 0 0 16px;
  }
  .ai-history__title {
    font: 600 clamp(28px, 3.6vw, 44px) / 1.15 var(--serif-display);
    color: var(--c-ink);
    margin: 0 0 16px;
    letter-spacing: -0.022em;
    max-width: 880px;
  }
  .ai-history__title .hl {
    color: var(--c-amber);
  }
  .ai-history__lede {
    font-size: 1.05rem;
    line-height: 1.6;
    color: var(--c-ink-2);
    max-width: 720px;
    margin: 0 0 48px;
  }
  .ai-history__rail {
    list-style: none;
    padding: 0;
    margin: 0 0 32px;
    display: grid;
    grid-template-columns: repeat(5, 1fr);
    gap: 0;
    border-top: 1px solid var(--c-rule);
    border-bottom: 1px solid var(--c-rule);
    position: relative;
  }
  @media (max-width: 980px) {
    .ai-history__rail {
      grid-template-columns: repeat(2, 1fr);
    }
  }
  @media (max-width: 580px) {
    .ai-history__rail {
      grid-template-columns: 1fr;
    }
  }
  .ai-rung {
    padding: 24px 20px;
    border-right: 1px solid var(--c-rule);
    display: flex;
    flex-direction: column;
    gap: 8px;
    position: relative;
    background: var(--c-bg);
    transition: background 0.2s ease;
  }
  .ai-rung:last-child {
    border-right: none;
  }
  @media (max-width: 980px) {
    .ai-rung:nth-child(2n) {
      border-right: none;
    }
  }
  .ai-rung:hover {
    background: var(--c-bg-elev, rgba(255, 255, 255, 0.02));
  }
  .ai-rung__year {
    font: 600 13px / 1 var(--ff-mono);
    color: var(--c-amber);
    letter-spacing: 0.16em;
  }
  .ai-rung__name {
    font: 600 18px / 1.25 var(--serif-display);
    color: var(--c-ink);
    margin: 4px 0 6px;
    letter-spacing: -0.01em;
  }
  .ai-rung--graph .ai-rung__name {
    color: var(--c-amber);
  }
  .ai-rung__sub {
    font-size: 0.82rem;
    line-height: 1.5;
    color: var(--c-ink-2);
    margin: 0 0 12px;
    flex: 1;
  }
  .ai-rung__line {
    font-size: 0.68rem;
    color: var(--c-ink-3);
    letter-spacing: 0.1em;
    margin: 0;
    text-transform: uppercase;
    padding-top: 8px;
    border-top: 1px dashed var(--c-rule);
  }
  .ai-history__closer {
    font-size: 1rem;
    line-height: 1.6;
    color: var(--c-ink-2);
    margin: 0;
    padding: 20px 24px;
    background: var(--c-bg-elev, rgba(255, 255, 255, 0.02));
    border-left: 3px solid var(--c-amber);
    border-radius: 0 4px 4px 0;
  }
  .ai-history__closer b {
    color: var(--c-amber);
    font-weight: 600;
  }
  .ai-history__closer a {
    color: var(--c-amber);
    text-decoration: underline;
    text-underline-offset: 3px;
  }
</style>
