/**
 * play — right-pointing triangle outline (single closed sub-path).
 * Meta category: media control — start playback.
 * Stroke-only on purpose per the chrome contract; the universal
 * "play" affordance is the triangle itself, no fill required.
 */
export const SVG_PATH = 'M6 4l14 8-14 8V4';
